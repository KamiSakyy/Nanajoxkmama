name: ci-test
on:
  push:
    branches: ["arena/**"]
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from $(uname -a)" && java -version && node -v
      - name: check android sdk
        run: ls "$ANDROID_HOME" && ls "$ANDROID_HOME/build-tools" || true
